package main

const (
	AgentAddr = ":9000"
	EtherAddr = ":9001"
)

func main() {
	// r := gin.Default()
	// root := r.Group(netflow.ROOT)

	

	// router.RouterGinInit(root, nil, gateway.NewHTTP(AgentAddr))



	// r.Run(EtherAddr)
}